# COSMOS External Services Integration

The external services are in charge for managing remote servers, cloud services and other external service platforms.

# Build

[indent=0]
----
	$ mvn clean install
----